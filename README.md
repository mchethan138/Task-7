# Task-7

1. Created a small SQLite database** (sales_data.db) and a table called sales
2. Inserted some fake sales data for products like Apple, Banana, and Orange
3. Wrote a simple SQL query to calculate:
   - Total quantity sold per product
   - Total revenue (quantity × price)
   - Used GROUP BY to group rows with the same product data
4. Pulled the results into Python using pandas and displayed the result using pd.read_sql_query
5. Displayed the data using a basic bar chart with matplotlib
